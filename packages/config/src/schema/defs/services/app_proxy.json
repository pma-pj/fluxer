{
	"app_proxy_service": {
		"type": "object",
		"description": "Configuration for the App Proxy service (frontend server).",
		"additionalProperties": false,
		"properties": {
			"port": {
				"type": "number",
				"description": "Port to listen on.",
				"default": 8773
			},
			"static_cdn_endpoint": {
				"type": "string",
				"description": "URL endpoint for serving static assets via CDN.",
				"default": ""
			},
			"assets_dir": {
				"type": "string",
				"description": "Filesystem directory containing static assets.",
				"default": "./assets"
			},
			"csp": {
				"description": "CSP (Content Security Policy) configuration to prevent cross-site scripting (XSS) and other attacks.",
				"$ref": "#/$defs/csp",
				"default": {}
			}
		}
	}
}
